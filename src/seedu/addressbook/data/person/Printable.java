package seedu.addressbook.data.person;

/**
 * An interface for generating a String for Name, Address, Phone and Email of a Person
 *
 */
public interface Printable {
	public abstract String getPrintableString();
}
